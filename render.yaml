services:
  - type: web
    name: akbros-factory-retail
    env: python
    plan: free
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt

    # Option A (simple Flask dev server for testing):
    startCommand: /opt/render/project/src/.venv/bin/python app.py

    # Option B (Gunicorn, recommended after it works):
    # startCommand: /opt/render/project/src/.venv/bin/gunicorn app:app --bind 0.0.0.0:$PORT

    envVars:
      - key: FLASK_SECRET
        generateValue: true
      - key: ADMIN_PASSCODE
        value: HKGOF2025@
      - key: ORDER_DB
        value: /tmp/orders_test.db



   
